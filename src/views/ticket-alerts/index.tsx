'use client'
import TicketAlerts from "./TicketAlert";
import { getServerMode } from '@core/utils/serverHelpers'

const TicketAlertsMain = ()=>{
    return ( 
       <TicketAlerts/>     
    ) 
}
export default TicketAlertsMain;
